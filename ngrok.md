# Terminal 1 - Run Vite dev server
npm run dev

# Terminal 2 - Run Laravel dev server  
php artisan serve --host=0.0.0.0 --port=8000

# Terminal 3 - Run ngrok to create HTTPS tunnel
# (from the folder where you extracted ngrok)
.\ngrok http 8000